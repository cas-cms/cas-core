Description:
    Generates new cas.yml and configures database for it.

Example:
    bin/rails generate cas:install

    This will add an /admin route for Cas and create a configuration file in
    config/cas.config.yml. Edit this file and run

        bin/rails cas:apply_config

