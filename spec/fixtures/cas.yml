config:
  uploads:
    cache_directory_prefix: 'cache'
    store_directory_prefix: 'uploads'
sites:
  mysite:
    sections:
      news:
        name: news
        type: content
        list_fields:
          - title
          - category
        fields:
          - category
          - tags
          - title
          - summary
          - text
          - images
          - files
          - tags
      biography:
        name: Biography
        type: content
        fields:
          - title
          - text
          - image
      agenda:
        name: Agenda
        type: content
        list_fields:
         - title
         - date
        fields:
         - title
         - local
         - date
         - text
