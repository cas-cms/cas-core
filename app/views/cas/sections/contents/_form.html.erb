

<%= form_for [@section, @content] do |f| %>
  <% # if Cas::SectionForm.new(@section.name, @section.site.name).has_field?(:title) %>
    <p>
      <%= f.label :Título %><br>
      <%= f.text_field :title %>
    </p>
  <% # end %>

  <p>
    <%= f.label :Texto %><br>
    <%= f.text_area :text %>
  </p>
  <p> <%= f.submit "Salvar" %> </p>
    
<% end %>